[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    unit: Fast unit tests (no external services)
    integration: Integration tests requiring Docker testcontainers
    api: FastAPI endpoint tests
    performance: High-load, concurrency, and isolation soak tests
filterwarnings =
    ignore::DeprecationWarning